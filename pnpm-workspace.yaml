# pnpm 11 reads build/override settings here, not from package.json.
# esbuild ships its platform binary via a postinstall build script; allow it
# so Vite/Vitest transforms work and the deps-status check stays clean.
allowBuilds:
  esbuild: true

overrides:
  esbuild: ^0.28.1
